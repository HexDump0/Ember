# 4 MB flash, single app slot, LittleFS volume mounted on `storage`
# (subtype `spiffs` is reused by esp_littlefs).
#
# Name,     Type, SubType, Offset,   Size,    Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  1700K,
storage,    data, spiffs,  ,         2000K,
